const userLoggedIn=true;
const debitCard=false;
const googleLoggedIn=false;
if(userLoggedIn && !debitCard && !googleLoggedIn){
    console.log("login successfully")
}else{
    console.log(`failed`);
    console.log(`failed`);
    console.log(`failed`);
    console.log(`failed`);
}
    